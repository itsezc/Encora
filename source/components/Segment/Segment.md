
Default section:

```js
<Segment>
	This is a basic segment block with a lot of text written inside to be shown as an example. You can insert a phrase, a paragraph or even an entire doctorine in a segment, it is a multi utility component. There are also multiple variations in segments which might be better suited for certain contexts.
</Segment>
```